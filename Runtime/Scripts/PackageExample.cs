using UnityEngine;


namespace PackageExample
{
    /// <summary>
    /// This is a simple example MonoBehaviour that can be used to demonstrate package functionality.
    /// </summary>
    /// <remarks>
    /// It is placed in the Runtime folder to ensure it is included in the build.
    /// </remarks>
    public class PackageExample : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}

